# AniSync 环境变量配置
# 复制此文件为 .env 并根据需要修改

# Bangumi API 反代地址（默认已内置 Deno Deploy 代理，一般无需修改）
# 如需使用自己的代理，取消注释并填入地址：
# BANGUMI_BASE_URL=https://你的代理.deno.net

# 数据库路径（默认 data/anisync.db，一般不用改）
# DATABASE_URL=data/anisync.db
